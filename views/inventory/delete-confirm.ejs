<style>
  main {
    max-width: 500px;
    margin: 2rem auto;
    padding: 2rem;
    background: #fff8f8;
    border: 1px solid #e57373;
    border-radius: 8px;
    box-shadow: 0 2px 8px rgba(230, 115, 115, 0.1);
  }
  h1 {
    color: #d32f2f;
    text-align: center;
    margin-bottom: 1rem;
  }
  p {
    color: #b71c1c;
    font-weight: bold;
    text-align: center;
    margin-bottom: 2rem;
  }
  form label {
    display: block;
    margin-top: 1rem;
    font-weight: 600;
    color: #333;
  }
  form input[type="text"] {
    width: 100%;
    padding: 0.5rem;
    margin-top: 0.25rem;
    border: 1px solid #e57373;
    border-radius: 4px;
    background: #fff;
    color: #333;
  }
  input[type="submit"] {
    display: block;
    width: 100%;
    margin-top: 2rem;
    padding: 0.75rem;
    background: #d32f2f;
    color: #fff;
    border: none;
    border-radius: 4px;
    font-size: 1.1rem;
    font-weight: bold;
    cursor: pointer;
    transition: background 0.2s;
  }
  input[type="submit"]:hover {
    background: #b71c1c;
  }
</style>
<main>
  <h1>Delete Inventory Item</h1>
  <p>Confirm Deletion - The delete is permanent.</p>
  <form action="/inv/delete" method="post">
    <label for="inv_make">Make</label>
    <input type="text" id="inv_make" name="inv_make" value="<%= inv.inv_make %>" readonly>

    <label for="inv_model">Model</label>
    <input type="text" id="inv_model" name="inv_model" value="<%= inv.inv_model %>" readonly>

    <label for="inv_year">Year</label>
    <input type="text" id="inv_year" name="inv_year" value="<%= inv.inv_year %>" readonly>

    <label for="inv_price">Price</label>
    <input type="text" id="inv_price" name="inv_price" value="<%= inv.inv_price %>" readonly>

    <input type="hidden" name="inv_id" value="<%= inv.inv_id %>">

    <input type="submit" value="Delete">
  </form>
</main>